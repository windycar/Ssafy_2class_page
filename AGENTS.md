# 학습 문제 추가·변경 규칙

학습 문제 세트를 추가하거나 문제은행 버전을 변경할 때 아래 작업을 반드시 함께 수행한다.

1. 문제 ID는 세트 안에서 영구적으로 고유하게 유지한다. 문제 의미가 바뀌어 기존 풀이와 호환되지 않으면 문제은행 버전과 attempt ID prefix를 올린다.
2. 모든 풀이 기록은 사용자별 로컬 pending 저장 후 Supabase에 업로드한다. 서버 조회가 성공하면 `reconcileRemoteProgress`를 사용해 서버 기록을 기준으로 교체하고, 요청 중 새로 생긴 로컬 답안만 보존한다.
3. 동기화 훅에는 `subscribeToStudyProgressRefresh`를 연결하여 포커스 복귀, 온라인 복귀, 다른 탭 변경, 주기 재조회가 동작하게 한다.
4. 모든 문제 세트에 `?mode=wrong` 오답 복습을 구현한다. 오답은 과거 전체 오답이 아니라 `getLatestAttemptsByQuestion`으로 계산한 문제별 최신 답안이 오답인 경우만 출제한다.
5. 오답 복습 구현 후 `src/config/studyReviewTracks.ts`에 세트를 등록한다. 약점 분석 화면은 이 설정을 기준으로 선택 항목을 표시한다.
6. 오답 선택 화면에는 Python, Web, AI Python, 주차별 문제 등 사용자가 풀 수 있는 모든 학습 문제 세트를 빠짐없이 등록한다.
7. 저장·초기화·오답 재풀이 회귀 테스트와 프로덕션 빌드를 통과시킨다.

새 문제 세트는 문제은행 파일만 추가하고 끝내지 않는다. 저장소, Supabase 동기화, 오답 모드, 복습 설정, 테스트까지 하나의 기능으로 완료한다.
